# Static SPA deploy: build client and serve from dist/public
[build]
  command = "npm run build"
  publish = "dist/public"

# SPA fallback: serve index.html for all routes so client-side routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
